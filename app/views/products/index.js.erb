<% if (params[:search]) && params[:page].nil? %>
  $("#products ul").html("<%= j render @products %>");
<% else %>
  $("#products ul").append("<%= j render @products %>")
<% end %>

$(".pagination").replaceWith("<%= j paginate @products %>");