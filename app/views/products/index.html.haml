%h2 Products
%nav.buttons
  = link_to 'Add a new product', new_product_path
- if @products.any?
  %ul.products
    - @products.each do |product|
      %li
        %a{:href => "#{product_path(product)}"}= product.name
- else
  %p
    %em
      There are no products on
      %strong Rainforest
      so far
